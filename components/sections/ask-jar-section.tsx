'use client';
import { useRouter } from 'next/navigation';import{ArrowRight,Sparkles}from'lucide-react';import{prompts}from'@/lib/cms/data';import{useState}from'react';
export function AskJarSection(){const router=useRouter();const[q,setQ]=useState('');const open=(p=q)=>{const s=p?`?q=${encodeURIComponent(p)}`:'';router.push('/ask-jar'+s)};return <section className="container py-8"><div className="console signal-bg grid gap-8 p-8 md:grid-cols-[32%_1fr]"><div><p className="gold text-sm font-black uppercase tracking-widest">Ask JAR Intelligence</p><h2 className="serif mt-4 text-5xl leading-none">Ask JAR.<br/>Get Intelligence.</h2><p className="mt-7 leading-7 text-stone-300">Ask a strategic question. Explore a premium advisory intelligence console shaped for regulated, complex and high-stakes environments.</p></div><div className="md:border-l md:border-[var(--gold)]/40 md:pl-8"><div className="flex gap-3 rounded-md border border-[var(--gold)]/60 p-3"><input value={q} onChange={e=>setQ(e.target.value)} className="min-w-0 flex-1 bg-transparent px-3 outline-none" placeholder="What strategic challenge can we help you solve?"/><button onClick={()=>open()} className="btn btn-primary shrink-0">Ask JAR <Sparkles size={14}/></button></div><p className="mt-6 text-xs font-black uppercase">Popular Questions</p><div className="mt-3 grid gap-3 md:grid-cols-2">{prompts.map(p=><button key={p} onClick={()=>open(p)} className="flex items-center justify-between rounded-md border border-[var(--gold)]/45 p-4 text-left text-sm hover:border-[var(--gold)]">{p}<ArrowRight className="gold" size={18}/></button>)}</div></div></div></section>}
