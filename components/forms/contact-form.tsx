'use client';
import{useState}from'react';
export function ContactForm(){const[done,setDone]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);await fetch('/api/leads',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(fd))});setDone(true)}if(done)return <div className="panel p-8"><h2 className="serif text-4xl">Thank you.</h2><p className="mt-4 text-[var(--muted-foreground)]">Your inquiry has been received for advisory review.</p></div>;return <form onSubmit={submit} className="panel grid gap-4 p-8"><input name="name" required placeholder="Name" className="rounded-md border border-[var(--border)] bg-transparent p-4"/><input name="email" required type="email" placeholder="Email" className="rounded-md border border-[var(--border)] bg-transparent p-4"/><input name="company" placeholder="Company" className="rounded-md border border-[var(--border)] bg-transparent p-4"/><textarea name="message" required placeholder="Message" rows={5} className="rounded-md border border-[var(--border)] bg-transparent p-4"/><input type="hidden" name="source" value="contact"/><button className="btn btn-primary">Submit Inquiry</button></form>}
